import React from "react";
import ReactDOM from "react-dom/client";
import TriggerBtn from "./TriggerBtn";

ReactDOM.createRoot(document.getElementById("trigger-btn-root")!).render(
  <React.StrictMode>
    <TriggerBtn />
  </React.StrictMode>
);
